# Codex Local Setup

This folder mirrors Prompt Structure skills for repository-local usage.

## If Codex uses global skills
You can also install globally:

```bash
mkdir -p "$CODEX_HOME/skills"
cp -R .codex/skills/* "$CODEX_HOME/skills/"
```
